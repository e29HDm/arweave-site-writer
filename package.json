{
  "name": "arweave-site-writer",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "contracts:build": "npm run clean && npm run contracts:build-ts",
    "contracts:build-ts": "node build_contracts.js",
    "clean": "rimraf ./dist-contracts",
    "contracts:deploy-test": "npm run ts-node backend/scripts/deploy-test-contract.ts",
    "contracts:test": "jest --config ./jest.contracts.config.ts"
  },
  "dependencies": {
    "vue": "^3.2.25"
  },
  "devDependencies": {
    "@types/jest": "^28.1.4",
    "@vitejs/plugin-vue": "^2.3.3",
    "esbuild": "^0.14.48",
    "jest": "^28.1.2",
    "replace-in-file": "^6.3.5",
    "rimraf": "^3.0.2",
    "ts-jest": "^28.0.5",
    "ts-node": "^10.8.2",
    "typescript": "^4.5.4",
    "vite": "^2.9.9",
    "vue-tsc": "^0.34.7"
  }
}
